{
  "name": "mini-erp-backend",
  "version": "1.0.0",
  "description": "Backend for Mini ERP Inventory Management System",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required for backend'",
    "dev": "node --watch server.js",
    "setup-db": "node scripts/setup_database.js",
    "ingest": "node scripts/ingest_data.js",
    "fix-schema": "node scripts/fix_schema.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "csv-writer": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^4.104.0",
    "pg": "^8.11.3",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}